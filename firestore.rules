
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For this single-tenant app, allow any authenticated user to read/write all data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
